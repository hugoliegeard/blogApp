{% extends "base.html.twig" %}

{% block title %}Accueil du Blog{% endblock %}

{% block body %}

    {#
        Depuis {{ app.user }} j'ai acces a l'ensemble
        des informations de mon utilisateur.
    #}

    <h1 class="display-4">
        {% if app.user %}
            Bonjour {{ app.user.fullName }},<br>
        {% endif %}
        Bienvenue sur mon Blog
    </h1>

    <img height="150" src="{{ asset('assets/images/jetee-au-bord-lac-hallstatt-autriche_181624-44201.avif') }}" alt="Blog">

    <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Atque cupiditate dolor exercitationem explicabo ipsa,
        maiores minima minus mollitia nam necessitatibus numquam officiis quis, quod quos, reprehenderit rerum tempora unde
        voluptatibus.</p>


    <div class="container">
        <div class="row">
            <h1 class="display-3 mb-5">Nos publications</h1>
            {% for post in posts %}
                <div class="col">
                    <div class="card">
                        <div class="card-body">
                            <img src="{{ post.image }}" alt="{{ post.title }}" class="img-fluid">
                            <h5 class="card-title">{{ post.title }}</h5>
                        </div>
                    </div>
                </div>
            {% else %}
                <div class="alert alert-info">
                    Pas d'article' pour le moment.
                </div>
            {% endfor %}

        </div> <!-- /.row -->
    </div> <!-- /.container -->

{% endblock %}


